// Ajuste esta URL depois de publicar o backend no Render.
// Em desenvolvimento local, aponta para o backend rodando em localhost.
export const API_URL = 'http://localhost:5000/api';

// Exemplo de URL de produção (troque pelo endereço gerado pelo Render):
// export const API_URL = 'https://pizzaflow-api.onrender.com/api';
