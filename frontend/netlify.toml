[build]
  base = "frontend"
  command = "npm install && npm run build -- --configuration production"
  publish = "dist/pizzaflow-frontend/browser"

# Redireciona todas as rotas para o index.html (necessário para o roteador do Angular)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
